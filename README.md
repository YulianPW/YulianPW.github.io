# Twitter Card 缓存刷新

如果站点分享图已经更新，但 Twitter 仍然显示旧缓存，可以使用 Twitter Card Validator 强制重新抓取页面：

1. 打开 [Twitter Card Validator](https://cards-dev.twitter.com/validator)。
2. 在输入框里填入你的页面链接。
3. 点击 `Preview card`。

这样会让 Twitter 重新抓取网页并更新缓存。
